import { useState } from 'react'
import { ArrowRight, Clock3 } from 'lucide-react'
import { EmptyState } from '../components/EmptyState'
import type { SessionDuration } from '../types'
const durations: SessionDuration[] = [10, 20, 30, 60, 120]
export function StudySession() { const [duration, setDuration] = useState<SessionDuration>(20); return <div className="page-stack"><section className="page-intro"><span className="eyebrow">Focus mode</span><h1>Study session</h1><p className="lede">Choose a timebox. Topics will be drawn from your connected curriculum.</p></section><section className="session-layout"><div className="card session-picker"><div className="section-heading"><div><span className="eyebrow">Session length</span><h2>How much time do you have?</h2></div><Clock3 size={20} /></div><div className="duration-list duration-list--large">{durations.map((value) => <button key={value} className={`duration-button ${duration === value ? 'duration-button--selected' : ''}`} onClick={() => setDuration(value)}>{value}<span>minutes</span></button>)}</div><button className="button button--dark button--full" disabled>Build {duration}-minute session <ArrowRight size={16} /></button></div><div className="session-preview"><span className="eyebrow">Next up</span><EmptyState title="No topics queued" description="Once topics are available, this session will suggest what to study here." /></div></section></div> }