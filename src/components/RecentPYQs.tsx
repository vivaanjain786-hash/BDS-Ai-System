import { ArrowUpRight, FileQuestion } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { Question } from '../types'

type RecentPYQsProps = { questions: Question[]; loading: boolean; error: string | null }
export function RecentPYQs({ questions, loading, error }: RecentPYQsProps) { return <section className="card recent-pyqs"><div className="section-heading"><div><span className="eyebrow">Exam preparation</span><h2>Recent PYQs</h2></div><FileQuestion size={19} /></div>{loading && <p className="question-muted">Loading mapped questions...</p>}{error && <p className="question-error">PYQs unavailable: {error}</p>}{!loading && !error && questions.length > 0 && <div className="recent-pyq-list">{questions.slice(0, 4).map((question) => <article className="recent-pyq" key={question.id}><div><span className="question-badge question-badge--pyq">{question.type === 'pyq' ? 'PYQ' : 'Sourced'}</span><span className="question-verification">{question.verification}</span><p>{question.prompt}</p><small>{question.source ?? 'Source not recorded'}</small></div><ArrowUpRight size={16} /></article>)}</div>}{!loading && !error && questions.length === 0 && <div className="question-empty"><strong>No verified PYQs yet</strong><span>Topic-mapped questions will appear here once added to the question bank.</span></div>}<Link className="text-link" to="/subjects">Browse subject questions <ArrowUpRight size={15} /></Link></section> }
